[pytest]
# pytest 9.1 defaults consider_namespace_packages=true; the plugin root has a
# loader-required __init__.py inside a hyphenated directory, which the walk-up
# then tries to import as a package. Disable so tests/ stays self-contained.
consider_namespace_packages = false
